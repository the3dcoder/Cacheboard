# Secrets. Never commit these.
server/.env
server/state.json

# Machine-local Claude Code settings (the shared ones live in settings.json)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
